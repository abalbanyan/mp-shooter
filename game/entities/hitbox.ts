/**
 * TODO: I think I can create a HitboxEntity which would be a property of
 * any collidable entity which would have a parameter for some arbitary onCollide()
 * which would trigger whatever appropriate state. This would simplify adding new collidable entities.
 */
